package chapter13.collection_01;

public class Lists {
    /**
     * List의 메서드(Collection에서 상속된 것은 제외)
     *
     * void add(int index, Object e) / boolean addAll(int index, Collection c) -> 지정된 위치(index)에 객체(e) 또는 컬렉션(c)에 포함된 객체들을 추가한다.
     * Object get(int index) -> 지정된 위치에 있는 객체를 반환한다
     * int indexOf(Object o) -> 지정된 객체의 위치를 순방향으로 순회하여 반환한다
     * int lastIndexOf(Object o) -> 지정된 객체의 위치를 역방향으로 순회하여 반환한다
     * ListIterator listIterator() -> List 객체에 접근할 수 있는 ListIterator 객체를 반환
     * Object remove(int index) -> 지정된 위치에 있는 객체를 삭제하고 삭제된 객체를 반환한다.
     * Object set(int index, Object e) -> 지정된 위치에 객체를 저장한다
     * void sort(Comparator c) -> 지정된 비교자(Comparator)로 List를 정렬한다.
     * List subList(int fromIndex, int toIndex) -> 지정된 범위 안에 있는 객체를 반환한다.
     * */


}
